# Shared secret — must match OCR_SERVICE_SECRET in the Next.js app
OCR_SERVICE_SECRET=change-me-shared-secret

# Path to a YOLOv8 license-plate detection model (.pt).
# If absent, the service falls back to running OCR on the full frame.
YOLO_MODEL=license_plate_detector.pt

PORT=8000
